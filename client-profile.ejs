<!DOCTYPE html>
<html>
<head>
    <title>Client Profile</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0; padding: 0;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f0f2f5;
            color: #333;
            padding: 40px 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            min-height: 100vh;
        }

        header {
            width: 100%;
            max-width: 800px;
            margin-bottom: 40px;
            text-align: center;
        }

        header h1 {
            font-size: 36px;
            color: #0077b6;
            font-weight: 600;
        }

        .container {
            background-color: white;
            max-width: 800px;
            width: 100%;
            padding: 30px 40px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.05);
            border-radius: 12px;
        }

        .client-info {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .info-heading {
            font-size: 22px;
            font-weight: 600;
            color: #0077b6;
        }

        .edit-button {
            font-size: 14px;
            background: none;
            border: none;
            color: #0077b6;
            cursor: pointer;
            transition: color 0.3s ease;
        }

        .edit-button:hover {
            text-decoration: underline;
            color: #005f8a;
        }

        p {
            font-size: 18px;
            margin-bottom: 12px;
            line-height: 1.5;
        }

        h2 {
            font-size: 24px;
            margin-top: 30px;
            margin-bottom: 15px;
            color: #0077b6;
            font-weight: 600;
        }

        button {
            background-color: #0077b6;
            color: white;
            padding: 12px 28px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 16px;
            font-weight: 600;
            transition: background-color 0.3s ease;
            margin-right: 10px;
            margin-top: 10px;
        }

        button:hover {
            background-color: #005f8a;
        }

        button a {
            color: white;
            text-decoration: none;
            display: inline-block;
            width: 100%;
            height: 100%;
        }

    </style>
</head>
<body>
    <header>
        <h1>Your Client Profile</h1>
    </header>

    <div class="container">
        <div class="client-info">
            <h2 class="info-heading">Client Information</h2>
            <button class="edit-button">
                <a href="/update-client?id=<%- client[0].c_id %>">Edit</a>
            </button>
        </div>
        <p><strong>Name:</strong> <%= client[0].c_name %></p>
        <p><strong>Address:</strong> <%= client[0].house_no %> <%= client[0].street %>, <%= client[0].city %>, <%= client[0].country %></p>

        <h2>Your Bookings</h2>
        <button>
            <a href="/client-booking?c_id=<%- client[0].c_id %>">View Bookings</a>
        </button>

        <h2>Search for a Service</h2>
        <button>
            <a href="/search-service?c_id=<%- client[0].c_id %>">Search Service</a>
        </button>
    </div>
</body>
</html>
