<!DOCTYPE html>
<html>
<head>
    <title>Provider Update</title>
    <style>
        body {
            font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        form {
            background-color: #fff;
            padding: 25px 30px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            width: 400px;
        }

        h1 {
            text-align: center;
            margin-bottom: 20px;
            color: #3498db;
            font-weight: 600;
        }

        label {
            font-weight: 600;
            display: block;
            margin-bottom: 6px;
            color: #333;
        }

        input[type="text"],
        input[type="password"] {
            width: 100%;
            padding: 10px 12px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 6px;
            font-size: 14px;
            box-sizing: border-box;
            transition: border-color 0.3s ease;
        }

        input[type="text"]:focus,
        input[type="password"]:focus {
            outline: none;
            border-color: #3498db;
            box-shadow: 0 0 5px rgba(52, 152, 219, 0.5);
        }

        input[type="submit"] {
            background-color: #3498db;
            color: white;
            padding: 12px 0;
            width: 100%;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-size: 16px;
            font-weight: 600;
            transition: background-color 0.3s ease;
        }

        input[type="submit"]:hover {
            background-color: #2980b9;
        }
    </style>
</head>
<body>

<form action="" method="post">
    <h1>Provider Update</h1>
    
    <label for="id">ID:</label>
    <input type="text" value="<%= provider[0].p_id %>" id="id" name="id" required readonly>
    
    <label for="name">Name:</label>
    <input type="text" value="<%= provider[0].p_name %>" id="name" name="name" required>
    
    <label for="house">House:</label>
    <input type="text" value="<%= provider[0].house %>" id="house" name="house" required>
    
    <label for="street">Street:</label>
    <input type="text" value="<%= provider[0].street %>" id="street" name="street" required>
    
    <label for="city">City:</label>
    <input type="text" value="<%= provider[0].city %>" id="city" name="city" required>
    
    <label for="country">Country:</label>
    <input type="text" value="<%= provider[0].country %>" id="country" name="country" required>
    
    <label for="service">Service:</label>
    <input type="text" value="<%= provider[0].service %>" id="service" name="service" required>
    
    <label for="password">Password:</label>
    <input type="password" id="password" minlength="5" maxlength="10" name="password" required>
    
    <input type="submit" value="Submit">
</form>

</body>
</html>
